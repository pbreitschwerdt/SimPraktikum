%% Initialize parameters for bridge crane

disp('Initialize parameter bridge crane...');

% Mass of cart
param.M = ..;    % [kg]
% Mass of gripper
param.m = ..;     % [kg]
% Gravitational constant 
param.g = ..;  % [m/s^2]
% Length of rope
param.l = ..;     % [m]
% Viscous friction
param.sigma = ..; % [N*m/(rad/s)]

disp('Parameter bridge crane initialized');

